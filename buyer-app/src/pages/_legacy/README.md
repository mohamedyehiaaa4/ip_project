# Legacy Buyer Pages

These files were moved from `src/pages/` because the active buyer frontend is implemented directly in `src/App.jsx`.

They are kept here only for historical reference and are not imported by the running app.
