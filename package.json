{
  "name": "market-place-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:buyer": "npm --prefix buyer-app run dev",
    "dev:seller": "npm --prefix seller-app run dev",
    "install:all": "npm install && npm --prefix backend install && npm --prefix buyer-app install && npm --prefix seller-app install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.11.3"
  }
}
